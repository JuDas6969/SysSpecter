# Logo assets

Expected files:

- `logo_icon.png` — square app icon (hooded figure in circle with heartbeat)
- `logo_wordmark.png` — full wide logo with "SysSpecter" text and tagline

The HTML report will embed these as base64 when present. Until the combined
source image is cut into these two files, the report falls back to a
text-only header.
